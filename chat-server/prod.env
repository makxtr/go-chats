# Production Configuration for Chat Service

# gRPC Server (internal container address)
GRPC_HOST=0.0.0.0
GRPC_PORT=50052

# PostgreSQL Connection (обновить с Cloud SQL данными)
# PG_DSN=host=your-cloud-sql-host port=5432 user=chat_user_prod password=change-this-strong-password dbname=chat_db_prod sslmode=require

# Environment
ENV=production

# Public URL (for reference only, not used by server)
# https://chat-service-rxpqkfxb3a-uc.a.run.app:443
